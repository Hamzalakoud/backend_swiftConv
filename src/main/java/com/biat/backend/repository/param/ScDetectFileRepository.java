package com.biat.backend.repository.param;

import org.springframework.data.jpa.repository.JpaRepository;

import com.biat.backend.model.param.ScDetectFile;

public interface ScDetectFileRepository extends JpaRepository<ScDetectFile, Long> {
}
